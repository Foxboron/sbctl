language: go
arch: amd64 
os: linux
dist: focal
go:
- 1.15.x
go_import_path: github.com/Foxboron
cache:
  directories:
    - $GOPATH/pkg/mod
script:
- make lint || travis_terminate 1
- make test || travis_terminate 1
branches:
  only:
  - master
notifications:
  email: false
